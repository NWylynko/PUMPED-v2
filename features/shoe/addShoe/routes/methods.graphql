mutation addNewShoe($shoes: [AddShoeInput!]!) {
	addShoe(input: $shoes) {
		shoe {
			shoeId
		}
	}
}

# query getBrands($searchTerm: String!) {
# 	queryBrand(filter: { name: { anyofterms: $searchTerm } }) {
# 		brandId
# 		name
# 	}
# }

query getBrands {
	queryBrand {
		brandId
		name
	}
}

mutation createBrand($brand: AddBrandInput!) {
	addBrand(input: [$brand]) {
		brand {
			brandId
			name
			website
		}
	}
}